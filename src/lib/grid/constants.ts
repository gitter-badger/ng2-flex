import { GridElementClasses } from "./gridElementClasses";

export const columnClasses = new GridElementClasses("col-top", "col-v-center", "col-bottom", "col-left", "col-h-center", "col-right");

export const rowClasses = new GridElementClasses("row-top", "row-v-center", "row-bottom", "row-left", "row-h-center", "row-right");

export const ItemsSeparationClasses = {
    All: "itm-separation-all",
    Between: "itm-separation-between"
}